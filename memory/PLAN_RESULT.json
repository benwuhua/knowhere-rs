{
  "task_id": "HNSW-P3-002",
  "job": "builder-loop",
  "task_type": "implementation_fix",
  "task_decision": "apply_minimal_level_multiplier_repair_and_rebench_remote",
  "workstream": "hnsw_graph_construction_quality",
  "gate_profile": "local_regression_then_remote_benchmark",
  "current_state": "OPT-036 后剩余差距调查中发现另一个实现级偏差：HnswIndex::new() 默认 level_multiplier 仍按 1/ln(m) 计算，导致高 M 层级塌缩且 IndexParams.ml override 未生效。本轮已用最小修复补齐热路径，并完成本地回归；后续判断必须回到 remote x86 benchmark。",
  "substage": "level_multiplier_fix_local_verified",
  "next_action": "在 remote x86 上重跑 HNSW recall-gated lane，至少比较 M=16/M=64 在修复前后的 recall、QPS 与层级统计；若收益有限，则继续聚焦 entry point 与 neighbor heuristic。",
  "required_checks": [
    "cargo test --test test_hnsw_level_multiplier -- --nocapture",
    "cargo test --lib test_random_level_distribution -- --nocapture",
    "cargo test --lib test_multilayer_structure -- --nocapture",
    "bash scripts/remote/test.sh --command \"cargo test --test bench_hnsw_cpp_compare -q\"",
    "bash scripts/remote/test.sh --command \"cargo test --test bench_hnsw_recall -q\""
  ],
  "deferred_checks": [
    "不要在 remote benchmark 之前宣称 level_multiplier 修复已经缩小 parity gap",
    "不要继续做 cheap 参数扫描；优先只验证修复是否改变 recall gate",
    "若 remote 收益不显著，则转向 entry point / graph construction 微调而不是扩散改动面"
  ],
  "updated_at": "2026-03-10T19:58:00Z"
}
